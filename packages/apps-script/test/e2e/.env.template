# E2E Test Environment Variables
# Copy this to .env and fill in your values

# ═══════════════════════════════════════════════════════════════
# CRITICAL: PRODUCTION SAFETY
# ═══════════════════════════════════════════════════════════════
# To prevent accidental production runs, list production Apps Script IDs:
# PRODUCTION_SCRIPT_IDS=prod-script-id-1,prod-script-id-2
# E2E tests will ABORT if APPS_SCRIPT_ID matches any production ID.

# Apps Script project ID (found in Apps Script editor > Project Settings)
# ⚠️  MUST be a test/staging deployment, NOT production!
APPS_SCRIPT_ID=your-apps-script-id-here

# Google Sheets template spreadsheet ID (used to create test spreadsheets)
TEST_TEMPLATE_SHEET_ID=your-template-spreadsheet-id

# Google Drive folder ID for test CSV files
TEST_FOLDER_ID=your-test-folder-id

# (Optional) Google Drive folder ID for test receipt files
# Required only if testing receipt import workflow
TEST_RECEIPTS_FOLDER_ID=your-receipts-folder-id

# Google service account credentials JSON
# Get from Google Cloud Console > IAM & Admin > Service Accounts
GOOGLE_SERVICE_ACCOUNT_KEY_JSON='{"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","private_key_id":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}'

# ═══════════════════════════════════════════════════════════════
# PRODUCTION DATA COPY (Advanced - use with caution)
# ═══════════════════════════════════════════════════════════════
# To copy production spreadsheet for testing:
# 1. Set PRODUCTION_BUDGET_DATA_SHEET_ID to your production sheet ID
# 2. Set ALLOW_PRODUCTION_COPY=true (explicit confirmation required)
# PRODUCTION_BUDGET_DATA_SHEET_ID=your-production-sheet-id
# ALLOW_PRODUCTION_COPY=true

# (Optional) Budget year for testing
# TEST_BUDGET_YEAR=2026

# (Optional) Skip cleanup to inspect test files after run
# SKIP_CLEANUP=true

# (Optional) Use mock Google API for faster development
# MOCK_GOOGLEAPI=1
